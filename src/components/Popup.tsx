const Popup = () => {
  return (
    <div className="popup" style={styles.main}>
      <div></div>
    </div>
  );
};

const styles = {
  main: {
    width: "300px",
    height: "300px",
    backgroundColor: "red",
  },
};

export default Popup;
